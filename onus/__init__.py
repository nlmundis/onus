"""Claims that carry their own proof.

A claim about an experiment, a rendered output, or a function's behaviour is accepted only on evidence fixed
before anyone looked: an exact statistical test under a pre-registered rule, an output a person signed off, or
an invariant checked over generated inputs. This scaffold carries the version and nothing else, so importing
it has no side effects and needs no third-party package; the subpackages arrive in v0.1.0.
"""

__version__ = "0.0.0"
